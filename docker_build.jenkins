#!groovy
// Run docker build
pipeline {
    agent {
        label 'master'
        }
    stages {
        stage("create docker image") {
            steps{
                echo "=========Start build============"
                                }
        }
    }
}
}
